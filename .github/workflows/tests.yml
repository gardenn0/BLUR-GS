name: Geometry tests
on: [push, pull_request]
permissions:
  contents: read
jobs:
  cpu:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      - run: pip install torch==2.3.0 --index-url https://download.pytorch.org/whl/cpu
      - run: pip install 'numpy<2' pillow pytest pypose==0.7.3 'scipy<1.15' 'plyfile<1.1.3' pytorch-msssim==1.0.0
      - run: python -m pytest -q -m 'not cuda'
